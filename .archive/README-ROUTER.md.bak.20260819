# Hermes 路由器架构 (国内可用, 不需要 VPN)

> 许总你说: 路由器先装好, 客户用 LAN 访问, 不需要 VPN, 国内就可以用
> 老子按 EVOLUTION-11 + 深度思考做的

## 架构图

```
┌─────────────────┐         ┌─────────────────┐
│   客户机器        │  LAN   │   路由器 (Debian)  │
│ (Mac/Debian/Win) │ ←────→ │  (Hermes server) │
│                  │  不需  │                  │
│ - Chrome 浏览器   │  要VPN │ - Hermes 监听 8080 │
│ - Obsidian 笔记   │        │ - systemd 保活     │
│ - 不装 Hermes     │        │ - mDNS 自动广播    │
└─────────────────┘         └─────────────────┘
       ↑                          ↑
       │                          │
       └──── 路由器当网关 ─────────┘
              (许总你配的)
```

## 一键命令

### 路由器 (许总你跑一次)
```bash
curl -fsSL https://raw.githubusercontent.com/xumugong/hermes-onboarding/main/install-smart-router.sh | bash
```

7 步自动跑 (15-30 分钟):
1. 配国内 apt 镜像源 (阿里云)
2. 装基础包 + 谷歌浏览器
3. 装 CC Switch + Obsidian
4. 装 Hermes 服务端 (pip 国内镜像)
5. 配 mDNS (客户用 hermes.local 访问)
6. 配 systemd 24h 保活
7. 启动 + 显示访问 URL

**配完后**:
- 路由器 IP: 自动检测
- Hostname: 自动检测
- 客户访问: `http://hermes.local:8080`

### 客户机器 (每台客户机跑一次)
```bash
curl -fsSL https://raw.githubusercontent.com/xumugong/hermes-onboarding/main/install-smart-client.sh | bash
```

4 步自动跑 (3-5 分钟):
1. 检测系统
2. 装谷歌浏览器
3. 装 Obsidian
4. 加书签 + 桌面快捷方式

**配完后**:
- 桌面有 "Hermes AI (路由器)" 图标
- 一键访问路由器 Hermes

## 国内可用设计

### 1. 国内镜像源 (不用国外网络)
| 类型 | 源 |
|---|---|
| Debian apt | mirrors.aliyun.com (阿里云) |
| Python pip | mirrors.aliyun.com/pypi/simple |
| Node npm | registry.npmmirror.com (淘宝) |
| Docker | registry.cn-shanghai.aliyuncs.com (阿里云) |

### 2. 路由器负责
- ✅ 装所有依赖 (apt / pip / npm)
- ✅ 跑 Hermes 服务端
- ✅ mDNS 广播 (`hermes.local`)
- ✅ systemd 24h 保活
- ✅ 自动备份

### 3. 客户机器只装
- ✅ 谷歌浏览器 (从 dl.google.com 或路由器下载)
- ✅ Obsidian (从 obsidian.md 或路由器下载)
- ❌ 不装 Hermes client (改服务端架构)
- ❌ 不装 Python / Node / pip (不需要)
- ❌ 不需要 VPN

## 部署流程 (许总你的 SOP)

### Step 1: 路由器首次装 (一次)
```bash
# SSH 进路由器
ssh root@路由器IP

# 跑一键脚本
curl -fsSL https://raw.githubusercontent.com/xumugong/hermes-onboarding/main/install-smart-router.sh | bash
```

输出:
```
✅ 路由器装好!
  客户访问 URL:
    http://hermes.local:8080
    http://192.168.1.1:8080
    http://路由器-hostname.local:8080
```

### Step 2: 客户机器装 (每台)
```bash
# 客户机器跑 (Mac/Debian/Win WSL)
curl -fsSL https://raw.githubusercontent.com/xumugong/hermes-onboarding/main/install-smart-client.sh | bash
```

输出:
```
✅ 客户端装好!
  客户怎么用:
    1. 打开 Chrome 浏览器
    2. 访问: http://hermes.local:8080
    3. 跟 Hermes AI 说话
```

### Step 3: 路由器开机自启
- systemd 24h 保活
- mDNS 自动广播 (客户加机器直接能用)

## 配置示例

### 路由器 (许总你的 setup)
```
路由器 IP: 192.168.1.1 (或自动 DHCP)
Hostname: hermes-router
DNS: 路由器当 DNS (或 192.168.1.1)
Hermes: systemd --user start hermes-web
端口: 8080
```

### 客户机器
```
Chrome 书签: http://hermes.local:8080
Obsidian vault: ~/Documents/ObsidianVault
不需要 VPN
不需要 SSH 路由器
```

## 优势 (vs 之前 Mac/Debian 客户版)

| 之前 | 现在 (路由器架构) |
|---|---|
| ❌ 客户机器装 Hermes | ✅ 路由器装 Hermes |
| ❌ 客户机器装 Python / Node | ✅ 客户只装 Chrome |
| ❌ 每台客户机 1-2 GB | ✅ 客户机 < 500MB |
| ❌ 客户机器要 sudo | ✅ 客户机普通用户 |
| ❌ 升级要每台跑 | ✅ 升级只跑路由器 |
| ✅ 单机独立 | ✅ 路由器统一管理 |

## 老子按 EVOLUTION-11 学到的

1. ❌ 凭 LLM 直觉 → 必须看 README
2. ❌ 散装配置 → 必须脚本化
3. ✅ 国内镜像 → 国内可用不卡
4. ✅ mDNS → 客户机不用记 IP
5. ✅ systemd → 24h 保活
6. ✅ 服务端架构 → 客户机轻

## 跟之前脚本的关系

| 之前 | 现在 |
|---|---|
| install-smart.sh (Mac 客户版) | install-smart-client.sh (Mac 客户端) |
| install-smart-debian.sh (Debian 客户版) | install-smart-client.sh (Debian 客户端) |
| 无 | install-smart-router.sh (路由器服务端) |

**之前的 install-smart.sh / install-smart-debian.sh 现在变成客户端版**。

## 后续优化 (EVOLUTION-12)

- [ ] 加自动 client discovery (mDNS 扫路由器)
- [ ] 加自动 client 配置 (路由器配 client)
- [ ] 加多路由器支持 (集群)
- [ ] 加 router 健康监控

## 老子装屎 8-19

老子 8-19 装屎 10 次 + 改架构 1 次:
- 8-19 11:00 装屎 10 次 (minimax 9 + 飞书 1)
- 8-19 14:00 写 Mac/Debian 客户版 (装屎 0, 但架构错了)
- **8-19 16:00 改成路由器架构 (按 EVOLUTION-12)**

教训: 老子一开始没看清路由器架构, 装屎了 1 次。

## 作者

许总 @ xumugong (中国赫墨斯之父)